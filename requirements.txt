# ── Core deep-learning stack ──────────────────────────────────────────────────
torch>=2.2.2
torchvision>=0.17.2

# ── Scientific / array utilities ──────────────────────────────────────────────
numpy>=1.24.1
scipy>=1.7.3
pandas>=1.3.5
PyWavelets          # pywt — DWT/DCT watermarking (dwt_dct)

# ── Image I/O and vision ──────────────────────────────────────────────────────
Pillow>=9.0.1
opencv-python>=4.10.0
scikit-image>=0.24.0

# ── HuggingFace ecosystem ─────────────────────────────────────────────────────
diffusers>=0.31.0
transformers>=4.40.1
peft>=0.7.1
huggingface-hub
safetensors

# ── Model / layer utilities ───────────────────────────────────────────────────
timm>=1.0.0
einops>=0.4.0
kornia              # omniguard
albumentations      # omniguard
fvcore              # omniguard (iml_vit_model.py)
basicsr             # editguard (Inv_arch, Subnet_constructor)
lpips>=0.1.4
torchmetrics        # trustmark

# ── Training / config infrastructure ─────────────────────────────────────────
lightning>=2.0
omegaconf>=2.1
six>=1.9            # trustmark

# ── Augmentation ──────────────────────────────────────────────────────────────
augly>=0.2.1        # ssl_watermarking

# ── Metrics / evaluation ──────────────────────────────────────────────────────
scikit-learn>=1.5.2
pycocotools>=2.0.8
tqdm>=4.63.0
matplotlib

# ── Misc ──────────────────────────────────────────────────────────────────────
PyYAML              # editguard options parsing
requests
